---
sidebar_position: 2
---

## Nord pirsm theme

```js
keyword
for (const foo of bar) {
  if (foo === "foobar") break;
  await foo;
}

// class
class Rectangle extends Square { /* ... */ } //className

//function
function isEven(number) {
	return Number(number) % 2 === 0;
}
const isOdd = (number) => !isEven(number);

//bollean
console.log(true === false); // prints false
console.log(true === !false); // prints true

//number
print(3.14159 * 42)
print(1e100 + .001j)
return foo & 0xdeadbeef

//string
let greeting = 'Hello World!';

//char
['A', 'z', '0', '-', '\t', '\u{2728}']

//symbol
#myFirstSymbol "#myFirstSymbol is a symbol in Smalltalk"
// regex
let entity = /&#x?[\da-f]{1,8};/;
//url
body {
	background: url(foo.png);
}
//operator
x += (y + 4 >> -z === w) ? b ** c : ~a;
//variable
echo $STRING
args=("$@")
echo ${args[0]} ${args[1]} ${args[2] //bash

//constant
fprintf(stdout, "hello world\n");

// ..property
{
	"data": { "labels": ["foo", "bar"], },
	"error": null,
	"status": "Ok"
}
//puctuation
def median(pool):
	copy = sorted(pool)
	size = len(copy)
	if size % 2 == 1:
		return copy[(size - 1) / 2]
	else:
		return (copy[size/2 - 1] + copy[size/2]) / 2

// important
 body {
	color: red !important;
}


```

```html
//comment
<!-- Here's a comment -->
<style>
  /* Here's another comment */
</style>
<script>
  // Here's yet another comment
</script>
```

```typescript
type SearchFunc = (source: string, subStr: string) => boolean;
```
